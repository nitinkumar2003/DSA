// Write a function to find the intersection (common elements) of two arrays.

function insertArr(){
  // type 1
    // insertedArray=arr1.filter(i=>arr2.includes(i))
    // return insertedArray;

    // type2
    // const set2=new Set(arr2)
    // return arr1.filter(i=>set2.has(i))
}
const result=insertArr([1, 2,8,7, 3], [2, 5,7,3, 4]);
console.log(result)